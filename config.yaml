# Aurum profile config — versioned in this repo.
# Secrets (API keys, bot tokens) go in ~/.hermes/profiles/aurum/.env

model:
  # Primary: lighter model (fewer 503s). Fallback: flash when lite is overloaded.
  # Override with: aurum config set model.default <slug>
  default: gemini-2.5-flash-lite
  provider: gemini
  base_url: https://generativelanguage.googleapis.com/v1beta

fallback_providers:
  - provider: gemini
    model: gemini-2.5-flash

agent:
  max_turns: 30
  reasoning_effort: low

memory:
  memory_enabled: true
  user_profile_enabled: true

compression:
  enabled: true
  threshold: 0.50

session_reset:
  mode: both
  idle_minutes: 1440
  at_hour: 4

platform_toolsets:
  cli: [hermes-cli]
  telegram: [hermes-telegram]

stt:
  enabled: true
